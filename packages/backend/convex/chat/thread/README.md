# Thread helpers (`chat/thread/`)

**Purpose:** Non-Convex modules shared by the chat thread feature. [`thread.ts`](../thread.ts) is the **only** file that registers Convex functions for `api.chat.thread.*`; this folder keeps that file readable.