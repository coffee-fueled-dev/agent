# Human agent toolkit (`agents/human/`)

**Scope:** Thread-agnostic building blocks for the **human** side of the agent system: the registered toolkit, static/runtime identity helpers, and client-side tool-call builders.