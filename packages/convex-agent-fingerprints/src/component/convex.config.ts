import { defineComponent } from "convex/server";

/** Convex component id; persisted agent/tool fingerprints (maps to internal “identity” tables). */
const component = defineComponent("agentFingerprints");

export default component;
