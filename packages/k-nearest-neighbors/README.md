# `@very-coffee/k-nearest-neighbors`

Pure TypeScript cosine k-nearest-neighbors graph builder.

## API

- `buildKnnGraph(entries, k)` -> `Map<string, Map<string, number>>`

## Scripts

- `bun run build`
- `bun run typecheck`
- `bun run test`
- `bun run bench`
