{
  "name": "@very-coffee/leiden",
  "description": "Pure TypeScript Leiden community detection.",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist --target node",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test",
    "bench": "bun ./bench.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
