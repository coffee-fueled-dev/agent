# `@very-coffee/leiden`

Pure TypeScript Leiden-style community detection on weighted undirected graphs.

## API

- `leiden(adjacency, resolution?)` -> `Map<string, number>`

## Scripts

- `bun run build`
- `bun run typecheck`
- `bun run test`
- `bun run bench`
