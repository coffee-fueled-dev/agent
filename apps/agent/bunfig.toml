[serve.static]
plugins = ["bun-plugin-tailwind"]
# Browser bundles: only these `process.env.*` names are inlined (Bun fullstack). `BUN_PUBLIC_*` must exist in env / `.env.local`.
env = "BUN_PUBLIC_*"
