# Agent app config

Listen address and ports come from **root** `.env.local` (`AGENT_LISTEN_HOST`, `AGENT_LISTEN_PORT`, or `HOSTNAME` / `PORT`) with defaults in [`packages/config`](../../packages/config). See [`config/env.example`](../../config/env.example).
